# Evaluation Artifacts

This directory stores evaluation outputs generated by tooling.

Retention policy:

- `latest/` is the mutable current snapshot.
- `baseline/` is for curated comparison baselines.
- Historical `cycle_*` runs should not be committed.

For full policy details see:

- `docs/EVALUATION_ARTIFACT_RETENTION_POLICY.md`
